==ERROR: AddressSanitizer: heap-use-after-free on address
READ of size 2 at......
freed by thread......
previously allocated by......

明确use-after-free的含义，heap上的一段内存空间free了之后又被使用了。通过堆栈找到具体的位置，找出哪里free之后又被哪里使用。
修改：在free之后，将指针对应的内存区域置NULL，重新使用的时候，重新生成获取。
